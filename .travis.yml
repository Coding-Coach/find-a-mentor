language: node_js
install:
- npm ci
node_js:
  - v11.6.0
cache:
  directories:
  - "$HOME/.npm"
script:
  - npm test
  - npm run predeploy
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $github_token
  local_dir: build
  on:
    branch: master